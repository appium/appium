import chai from 'chai';

const {assert} = chai;

describe('Dummy test', function () {
  it('should run a temp test', async function () {
    assert.equal(1+1, 2, 'One plus one should equal two');
  });
});